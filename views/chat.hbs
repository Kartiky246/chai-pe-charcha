<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Chat with {{speaker.name}}</title>
  <link rel="stylesheet" href="/css/chat.css" />
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <!-- Chat container -->
  <main class="chat-container">
    <div class="chat-header">
        <a href="/" class="chat-back">
             ⬅️
        </a>
      <img src="{{speaker.avatar}}" alt="{{speaker.name}}" class="chat-avatar" />
      <h2 class="chat-name">{{speaker.name}}</h2>
    </div>

    <div class="chat-messages" id="chat-messages">
      <!-- messages will be appended here dynamically -->
    </div>

    <form class="chat-input-bar" id="chat-form">
      <input 
        type="text" 
        id="chat-input" 
        placeholder="Type your message..." 
        autocomplete="off" />
      <button type="submit">Send</button>
    </form>
  </main>

  <script>
    window.speakerId = "{{speaker.id}}";  <!-- Pass speaker id from server -->
  </script>
  <script src="/js/chat.js"></script>
</body>
</html>
